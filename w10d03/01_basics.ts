let username: string | number | boolean = 'alice';
username = 42;

// comment
username = false;

const numbers: (string | number)[] = [];

numbers.push(4);
numbers.push('hello');

const myVar: string | number | undefined = numbers.pop();
